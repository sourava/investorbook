import { makeStyles } from "@material-ui/core/styles";

export const searchBoxStyles = makeStyles((theme) => ({
  searchBoxContainer: {
    display: "flex",
    alignItems: "center",
  },
  searchBox: {
    marginRight: "10px",
  },
}));
